# DevOps Project

- Jenkinsfile
- docker-compose.yml
- app
  - Dockerfile
  - requirements.txt    
  - app.py

Simple flask app to lsit & create users in a MySQL database.

## Prerequisites

- Docker
- Docker Compose
- Jenkins

Uses jenkins to build and deploy the app.